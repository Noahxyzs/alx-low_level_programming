#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entery
 *
 * Return: Always 0
 */
int main(void)
{
	char n;

	int i;

	n = 'a';
	i = 0;
	while
		(i < 10) {
			putchar(i + '0');
			i++;
		}
	while
		(n <= 'f') {
			putchar(n);
			n++;
		}
	putchar('\n');
	return (0);
}
